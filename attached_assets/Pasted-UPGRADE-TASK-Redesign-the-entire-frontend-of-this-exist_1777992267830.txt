UPGRADE TASK: Redesign the entire frontend of this existing Team Task Manager 
to MAANG-level (Google/Linear/Notion/Vercel-tier) UI/UX. 
DO NOT rewrite backend logic or database schema. 
Only upgrade: styling, layout, components, animations, and UX flow.
Work file-by-file efficiently.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎨 DESIGN SYSTEM — Apply globally first
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Colors (CSS variables in :root):
  --bg-primary: #0a0a0f
  --bg-secondary: #111118
  --bg-card: #16161f
  --bg-hover: #1e1e2a
  --border: #2a2a3a
  --border-subtle: #1e1e28
  --accent: #6c63ff
  --accent-hover: #7c74ff
  --accent-glow: rgba(108,99,255,0.15)
  --text-primary: #f0f0ff
  --text-secondary: #8b8ba7
  --text-muted: #555570
  --success: #22c55e
  --warning: #f59e0b
  --danger: #ef4444
  --info: #3b82f6

Typography:
  Font: Inter (import from Google Fonts)
  Base: 14px, line-height 1.6
  Headings: font-weight 600–700, letter-spacing -0.02em

Spacing: 4px base unit — use multiples (4,8,12,16,20,24,32,40,48)
Border-radius: cards=12px, buttons=8px, inputs=8px, badges=6px, pills=999px
Shadows: 0 1px 3px rgba(0,0,0,0.4), 0 8px 24px rgba(0,0,0,0.3)
Transitions: all 0.15s ease for hovers, 0.25s ease for panels

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔐 AUTH PAGES (Login & Signup)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Layout: Full-screen split — left side shows animated gradient mesh 
background with app name + tagline; right side has the form.
On mobile: full-screen form with logo on top.

Form card: bg-card, border, 40px padding, 400px max-width
- Logo: gradient icon (purple→blue) + "TeamFlow" bold text
- Input fields: bg-secondary, border, 44px height, icon prefix (lucide icons),
  focus ring with accent color glow (box-shadow: 0 0 0 3px var(--accent-glow))
- Password: toggle show/hide eye icon
- Primary button: full-width, 44px, bg-accent, hover scale(1.01), 
  loading spinner state while submitting
- Divider with "or continue with" text
- Social login buttons (styled, even if not functional)
- Error messages: red text with shake animation
- Switch link: "Don't have account? Sign up" styled as accent color

Animated background (left panel):
  CSS animated gradient: background-size 400% 400%, 
  @keyframes gradientShift over 8s infinite

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🧭 SIDEBAR NAVIGATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Width: 240px fixed, collapsible to 64px (icon-only) on toggle
Background: bg-secondary, right border: 1px solid var(--border)

Top section:
- App logo + name, collapse toggle button (ChevronLeft icon, rotates on collapse)
- User avatar (initials-based colored circle) + name + role badge

Navigation items:
- Icon (lucide) + Label
- Active state: bg-accent-glow, left border 2px accent, text-primary
- Hover: bg-hover, text-primary, transition
- Items: Dashboard, Projects, My Tasks, Team, Settings
- Admin-only section label "Admin" with lock icon, then: User Management

Bottom: 
- Help & Support link
- Logout button (danger color on hover)
- Collapsed mode: show only icons with tooltips on hover

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 DASHBOARD PAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Header: "Good morning, [Name] 👋" + date subtitle + "New Task" CTA button

Stat Cards Row (4 cards):
  Each card: bg-card, border, 20px padding, 12px radius
  - Icon in 40x40 accent-bg rounded square
  - Large number (28px bold) + label (text-secondary)
  - Trend indicator: green↑ or red↓ with percentage
  Cards: Total Tasks, In Progress, Completed Today, Overdue

Activity + Tasks split layout (2/3 + 1/3):

Left — Task List:
  - Filter tabs: All / My Tasks / Overdue (pill tabs, active=accent bg)
  - Each task row: checkbox, task name, project badge (colored pill), 
    assignee avatar, due date, priority dot, status badge
  - Priority dots: red=high, amber=medium, blue=low
  - Status badges: rounded pills, color-coded
  - Hover: bg-hover row highlight
  - Overdue tasks: left border 3px danger color
  - Empty state: illustration + "No tasks yet" message

Right — Activity Feed:
  - "Recent Activity" heading
  - Timeline list: avatar + action text + timestamp
  - Color-coded left border per action type (create/update/complete)

Progress Section:
  - Project progress cards: project name, team avatars stack, 
    progress bar (gradient fill, animated on load), percentage + task count

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📁 PROJECTS PAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Header: "Projects" title + "New Project" button (accent, + icon)
Toggle: Grid view / List view icons (active=accent)

Grid view — Project cards:
  - Top color bar (random from 6 preset accent colors per project)
  - Project name (18px bold) + description (2 lines, ellipsis)
  - Stats row: task count, member count, due date
  - Progress bar (thin, 6px, rounded)
  - Avatars row (stacked, max 4 + overflow count)
  - Status badge: Active / On Hold / Completed
  - Three-dot menu: Edit, Archive, Delete (admin only)
  - Hover: card lift (translateY -2px) + shadow increase

List view — Table:
  Columns: Name, Status, Progress, Members, Tasks, Due Date, Actions
  Sortable columns with arrow icons
  Alternating row hover bg

"New Project" Modal:
  - Overlay backdrop (blur + dark)
  - Modal: 520px wide, bg-card, 32px padding, border-radius 16px
  - Form: name, description (textarea), color picker (6 swatches), 
    due date, member selector (search + add chips)
  - Footer: Cancel + Create buttons

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ TASKS PAGE (Kanban + List)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

View toggle: Kanban Board / List View

Kanban Board:
  Columns: To Do / In Progress / In Review / Done
  Column header: title + count badge + "+" add task button
  Column bg: bg-secondary, rounded 12px, min-height 400px
  
  Task Cards:
  - bg-card, border, 12px radius, 16px padding
  - Priority color indicator (top-left corner 4px dot or left border)
  - Task title (bold), description preview (2 lines)
  - Tags/labels: small colored pills
  - Bottom row: assignee avatar + name, due date, comment count icon
  - Hover: border-color → accent, shadow, translateY -1px
  - Drag handle icon (appears on hover)

  Add Task inline: clicking "+" shows inline input card with just 
  title field + Save/Cancel — no modal needed

Task Detail Slide Panel (right side, 480px):
  Opens on task click, slides in from right (transform translateX)
  - Full task title (editable inline)
  - Status dropdown, Priority selector, Assignee picker
  - Description (rich text or textarea)
  - Due date picker
  - Subtasks checklist (add/remove)
  - Comments section: avatar + comment + timestamp
  - Activity log at bottom

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👥 TEAM PAGE (Admin view)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Header: member count + "Invite Member" button

Member cards (grid):
  - Large avatar (initials, colored background per name hash)
  - Name, email, role badge (Admin=purple, Member=blue)
  - Stats: tasks assigned, tasks completed
  - "Change Role" dropdown (admin only)
  - "Remove" button (danger, admin only)

Invite Modal: email input + role selector + Send Invite button

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🌐 TOP HEADER BAR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Height: 56px, bg-secondary, border-bottom, sticky top

Left: Page breadcrumb (Home > Projects > Project Name)
Center: Global search bar (280px, cmd+K shortcut hint, search icon)
Right: 
  - Notification bell with unread count badge (red dot)
  - Notification dropdown panel (list of recent activities)
  - User avatar → dropdown: Profile, Settings, Logout

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ MICRO-INTERACTIONS & POLISH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Skeleton loaders for all data-fetching states (grey animated shimmer)
- Toast notifications (top-right, slide in): success/error/info variants
- Button loading states: spinner replaces text, disabled during submit
- Form validation: inline red error text, input border → danger color
- Empty states: SVG illustration + message + CTA button
- Smooth page transitions (opacity fade 0.2s)
- Task completion: strikethrough animation + checkmark bounce
- Number counters animate up on dashboard load
- Tooltips on all icon-only buttons
- Mobile: sidebar becomes bottom sheet drawer, cards stack full-width

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📱 RESPONSIVE BREAKPOINTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Desktop (>1024px): Full sidebar + content
Tablet (768–1024px): Collapsed sidebar (icon only)
Mobile (<768px): Hidden sidebar, hamburger menu, 
                  single-column layout, bottom nav bar

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚡ IMPLEMENTATION ORDER (follow this sequence)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Apply CSS design system variables globally
2. Install lucide-react (or lucide icons)
3. Upgrade Auth pages
4. Upgrade Sidebar
5. Upgrade Top Header
6. Upgrade Dashboard
7. Upgrade Projects page + modal
8. Upgrade Tasks (Kanban board + detail panel)
9. Upgrade Team page
10. Add toast, skeleton, and empty state components
11. Apply responsive styles last

Keep all existing API calls and state logic intact.
Only change JSX structure and CSS/styling.
Use existing component files — do not create unnecessary new files.